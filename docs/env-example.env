# Clerk environment example for Vite + React
# Copy this file to .env.local and fill in the values.
# IMPORTANT: For Vite, only variables prefixed with VITE_ are exposed to the client.

# Clerk publishable key (public, safe for client)
VITE_CLERK_PUBLISHABLE_KEY=

# If you will use Clerk backend APIs from a server component or server process, keep this secret server-side only.
# Not used directly in this Vite client app, but documented here for completeness.
CLERK_SECRET_KEY=

# Backend (FastAPI) email configuration
SENDGRID_API_KEY=
EMAIL_FROM="Helen <no-reply@localhost>"

